---
  - name: "Setting up the homeserver"
    connection: local
    hosts: localhost

    # Become root user, which is needed for most tasks
    become: yes
    become_user: root

    vars_files:
        - ansible/vars/main.yaml

    tasks:
    - import_tasks: ansible/tasks/zfs.yaml
